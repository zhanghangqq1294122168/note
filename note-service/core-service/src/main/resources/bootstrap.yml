server:
  port: 8763
spring:
  application:
    name: core-sevice
  # 配置中心
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: note-config
      profile: dev
      name: core
  jackson:
    default-property-inclusion: non_null
